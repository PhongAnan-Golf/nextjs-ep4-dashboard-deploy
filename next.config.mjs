/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'store.storeimages.cdn-apple.com', 'picsum.photos', 'static.vecteezy.com', 'www.vecteezy.com'],
  },
};

export default nextConfig;
